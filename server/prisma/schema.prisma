datasource db {
    provider = "sqlite"
    url = "file:./data.db"
}

generator client {
    provider = "prisma-client-js"
}

model Data {
    id Int @id @default(autoincrement())
    loggedAt DateTime @default(now())
    data String

}